// ? "valor verdadeiro": "valor falso"

const pontuacao = 999;
const nivel = pontuacao >= 1000 ? "Usuario Vip" : "Usuario normal";

const corUsuario = null;
const corPadrao = corUsuario || "Preta"; //Define um valor padrao
