const produto = { nome: "Camiseta", preco: 2 };
// const outraCoisa = { ...produto }; //Utiliza para copiar
// const caneca = Object.assign({}, produto, { material: "porcela" }); //Utiliza para copiar
// Object.freeze(produto) trava as propriedades

// console.log(Object.getOwnPropertyDescriptor(produto, "nome")); //Retorna as propriedades writable, configurable
// console.log(Object.keys(produto)); //Retorna chave
// console.log(Object.values(produto)); //Retorna valor
// console.log(Object.entries(produto)); //Retorna chave, valor
